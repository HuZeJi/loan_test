services:
  db:
    image: mysql:8.0
    container_name: chn_test_db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: chn_test
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      # Se ejecuta automaticamente al iniciar el contenedor por primera vez,
      # creando las tablas descritas en el Diagrama ER.
      - ./SQL_Script/schema.sql:/docker-entrypoint-initdb.d/schema.sql:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-proot"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./be_test
    container_name: chn_test_backend
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      DB_PORT: "3306"
      DB_NAME: chn_test
      DB_USERNAME: root
      DB_PASSWORD: root
    ports:
      - "8080:8080"

volumes:
  db_data:
