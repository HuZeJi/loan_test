server {
    listen ${PORT};
    root /usr/share/nginx/html;
    index index.html;

    # Angular router uses client-side routes, so unknown paths fall back to index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
